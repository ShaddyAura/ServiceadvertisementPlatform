namespace ServAd.ApiService.Data.Enums
{
    public class DashboardEnums
    {
        public enum VerificationStatus { Pending, Verified, Rejected }
        public enum DocumentType { Citizenship, License, Passport, NIN }
        public enum BookingStatus { Pending, Confirmed, Completed, Cancelled }
    }
}
